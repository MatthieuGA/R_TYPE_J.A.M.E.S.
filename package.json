{
  "name": "r-type",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{cpp,hpp,c,h,cc}": [
      "clang-format -i",
      "cpplint --repository=. --quiet --output=vs7 --filter=-legal/copyright,-build/c++17,+build/c++23,-runtime/references"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "gitmojis": "^3.15.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}