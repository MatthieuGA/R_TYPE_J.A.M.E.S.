{
  "name": "r-type",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{cpp,hpp,c,h,cc}": [
      "clang-format -i",
      "py -3 -m cpplint --repository=. --quiet --output=vs7 --filter=-legal/copyright,-build/c++17,+build/c++20,-whitespace/indent_namespace,-runtime/references"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "gitmojis": "^3.15.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}